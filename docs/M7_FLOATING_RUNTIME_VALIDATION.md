# Milestone 7 consolidated Windows runtime check

Use one real Windows 10/11 x64 session after the final Milestone 7 source candidate has passed both exact-head PR CI and resulting-main CI. The current source is `50cef428785ff522ab614eae3f4299241b69fb9f`, resulting-main CI #495 / run `35974887744`, runtime artifact `10798282284` (`narro-m1-runtime-harness-windows-x64`), digest `sha256:da84e0ec12865bb088835c6279b04229db6848673de332d20a45ea90d97f3aa3`. Verify the running executable comes from that artifact; an older installed Narro binary cannot validate this source. Record the Windows version, monitor layout/scaling, taskbar location, and app used for the borderless test. Keep each result as `PASS`, `FAIL`, or `NOT RUN`; screenshots or a short capture are useful for visual failures. `HANDOFF.md` carries the current artifact if a later source candidate supersedes this one.

Start with one active task and note its session ID and displayed elapsed time. Preserve that task through the mode, shortcut, resize, and monitor checks so session continuity can be checked once across the sequence. Also test reduced motion for the finite transitions and Find Timer pulse.

| Gate | Physical action | Pass evidence |
| --- | --- | --- |
| 7: compositor and transitions | Switch Panel → Timer → Panel from the configured right side. In Timer, expand/collapse several times, including while subtasks and the action strip are visible. Check the normal product-size window for a horizontal scrollbar. | No left/staging flash, stale or duplicated action-strip pixels, old expanded pixels in collapsed geometry, horizontal scrollbar, or abrupt return flicker; right-side Panel return and timer/session continuity hold. |
| 8: alternate shortcut | Press Ctrl+Shift+T repeatedly from both modes, then while a mode/resize transition is still pending. Exercise the existing shortcut conflict/retry diagnostics if another app owns the key combination. | One mode change per accepted request, no duplicated session or overlap; a conflict is reported and recoverable. |
| 9: Find Timer | Move the Timer away, press Ctrl+Shift+P, and repeat with the Timer hidden. Also press it while the Panel is showing. | In Timer mode the existing window becomes visible/focused and receives one restrained pulse per request; the pulse ends, including with reduced motion. In Panel mode the shortcut leaves the Panel and session unchanged. |
| 10: last position | Drag Timer to a distinct position, return to Panel and reopen, quit/restart, then change resolution/scaling or disconnect/reconnect a secondary monitor. | Position restores on the same available monitor when possible, otherwise lands fully in a visible work area; no off-screen trap or duplicate window. |
| 11: topmost | Place Timer over a normal maximized app, then over a borderless full-screen app and switch focus back and forth. If available, separately try an app's exclusive full-screen mode. | Record actual stacking and taskbar behavior for each app/mode. Do not infer exclusive full-screen behavior from the borderless result. |
| 12: bottom/taskbar | Drag collapsed Timer to the bottom of the work area, including a secondary monitor and a non-default taskbar position if available; expand, use the lowest action/subtask controls, then collapse. | Expanded outer window and controls remain inside the selected monitor work area; collapse remains usable and session state is unchanged. |
| 13–14: idle motion and resources | With `main` destroyed via the diagnostic harness, observe collapsed idle and expanded idle after transitions/pulse finish. Run the existing floating measurement harness for both, then for a running timer. | No continuing decorative motion at idle; capture three valid settled runs per idle state and compare median CPU/working set/private bytes with the Milestone 1 baseline. Record running-timer cost separately. |

For item 14, use `scripts/measure-floating.ps1` as specified in `docs/M1_FLOATING_PERFORMANCE_MEASUREMENT.md`, with separate `-OutputDirectory` paths for collapsed idle, expanded idle, and timer-running cases. Confirm `steadyStateValid: true` and zero churn intervals for each run. The Milestone 1 baseline medians were 0.026% of one core, 396.21 MiB aggregate working set, and 325.40 MiB aggregate private bytes. Do not treat hosted CI runner resource numbers as the physical result.

Windows presentation behavior can differ by graphics mode. Microsoft documents that DirectFlip/Independent Flip can bypass desktop composition and can change how other desktop content is composed above a full-screen surface: [DXGI flip model guidance](https://learn.microsoft.com/en-us/windows/win32/direct3ddxgi/for-best-performance--use-dxgi-flip-model). This is a reason to record the exact app and display mode for item 11, not a claim that every exclusive full-screen app must hide the Timer.

Record the completed matrix and any captures in a new immutable `work-log/` entry. Keep top-level TODO gates open wherever physical evidence is missing or fails.
