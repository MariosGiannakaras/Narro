# HANDOFF.md

Canonical continuation point. Read `AI_START_HERE.md`, `AGENTS.md`, `ENGINEERING_QUALITY.md`, `AGENT_WORKFLOW.md`, active M7 `TODO.md`, relevant `STATUS.md`, `docs/BLITZIT_HISTORY_RISK_INDEX.md` when applicable, the newest immutable `work-log/` entry, and live PR/CI state before implementation.

The Narro worktrees are grouped under the Desktop folder `E:\\SystemFiles\\Desktop\\Narro Project`. Inspect branch/dirty state before switching: the `Narro-m7-idle-performance` worktree previously contained three untracked icon outputs generated by local `npm run build`; they were intentionally excluded from source/docs commits. GitHub `main` remains the durable source truth.

## CURRENT MILESTONE

Milestone 7 — Floating Timer mode. Milestones 1–6 are complete. M7 items 1–6 and 13–14 are validated; item 7 still requires physical Windows compositor/reduced-motion verification. Do not advance to M8 before the remaining M7 acceptance conditions are observed.

## CURRENT VALIDATED SOURCE

Validated source baseline: `c875b4894e90cc75c04c9ff9508ef1dc1a176ad5` (guarded squash merge of PR #145). Its tree `67ced4d48545772a48b5452f686f3b15127ce5a2` is identical to exact PR head `f901fa907b550e921daf32123ec84a49aebb7006`.

PR #145 exact-head Windows CI #513 / run `36173387766`: **PASS**. Repository Preflight, Windows visual regression, Tauri Release, visual artifact upload and diagnostic runtime artifact upload all passed.

Resulting-main Windows CI #514 / run `36176196311` / job `108207271685`: **PASS** on exact source SHA `c875b4894e90cc75c04c9ff9508ef1dc1a176ad5`.
- runtime artifact `10882911552`, digest `sha256:b73a1dafd98af88eb1f76886508dd07786005e121fd5f001205085eccf1f483e`;
- visual artifact `10883280101`, digest `sha256:226331d647043da70f7ee3c7af9a489da38e0407060eb3cfd8c1e5a252cebd13`.

This handoff/tracking reconciliation is documentation-only. Any later markdown-only tracking commit does **not** replace the validated source baseline above.

## COMPLETED CAPABILITY IN THIS SLICE

PR #145 fixes the reproduced blank/staging interval without adding timing-delay heuristics:
- native Panel/Timer transition APIs are split into hidden `prepare_*` and visible `reveal_*` boundaries;
- React publishes the target mode synchronously with `flushSync` while the native host is hidden;
- the target `FocusSurfaceTransition` root can mount as prepainted so reveal does not expose an empty entry state;
- the coordinator order is prepare target → publish target → presented-frame barrier → reveal target;
- after target preparation, frame/reveal/cancellation failure recovers previous mode through the same sequence;
- native Panel prepare rollback restores prior size, position, topmost, skip-taskbar and visibility;
- executable coordinator tests cover success, prepare failure, frame failure, reveal failure, cancellation and recovery failure;
- no high-frequency geometry loop, polling clock, renderer timer/session authority or speculative fixed delay was added.

CI history for the corrective branch:
- #510 failed only because a new static test used LF-specific text against Windows CRLF checkout;
- #511 failed only because a static assertion expected a stale source spelling after the stronger recovery snapshot implementation;
- #512 passed frontend/build behavior and failed only `cargo fmt --check` on one formatting line;
- #513 passed the final exact PR head;
- #514 passed the resulting main source.

## INVARIANTS

Preserve:
- exactly the normal two-webview model: `main` plus reusable `focusSurface`; no second focus/timer webview;
- Rust/native authority for window geometry, monitor/work-area/DPI placement and presentation mode;
- authoritative timer/session/task/scheduling/persistence state outside renderer memory;
- live session continuity across Panel↔Timer, expand/collapse, shortcut and window lifecycle changes;
- finite opacity/transform motion only; no continuous decorative animation or high-frequency JS native-geometry loop;
- reduced-motion usability;
- complete failure rollback rather than partially publishing a new presentation;
- persistence-first domain mutations and all previously validated M1–M6 correctness invariants.

## UNFINISHED M7 WORK

Item 7 is **not complete** until physical Windows evidence confirms the source correction:
- continuous Panel→Timer and Timer→Panel capture has no blank/pale/staging frame or abrupt return flicker;
- Windows animations Off/reduced-motion retest passes both directions;
- normal-size focus surface has no horizontal scrollbar;
- expanded/collapsed cycles retain no stale/duplicated expanded pixels.

Other existing M7 physical gates remain open as tracked in `TODO.md` and `docs/M7_FLOATING_RUNTIME_VALIDATION.md`: native-hidden/Panel shortcut follow-up, secondary-monitor/topology/no-saved-position recovery, non-default taskbar/constrained-work-area/high-DPI cases, and independent borderless/optional exclusive-fullscreen stacking. Do not repeat already-settled performance runs unless a performance-relevant source change occurs.

## EXACT NEXT ACTION

1. Recheck `main`, open PRs and CI before touching source.
2. Use CI #514 runtime artifact `10882911552` for physical Windows Panel→Timer and Timer→Panel continuous capture.
3. Repeat with actual Windows animations Off/reduced motion, then restore the OS setting.
4. Record scoped PASS/FAIL evidence in a new immutable work log and update `TODO.md`/`STATUS.md`/`HANDOFF.md`.
5. If the no-flash criterion still fails, diagnose only the observed frame sequence and fix it as another narrow M7 item-7 slice with executable regression coverage and exact-head Windows CI. If it passes, continue the remaining M7 physical matrix; do not start M8.

No unresolved product-policy decision blocks the next action. Remaining blockers are physical Windows/environment validation conditions, not a product choice.
