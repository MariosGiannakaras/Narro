# HANDOFF.md

Canonical continuation point. Read `AI_START_HERE.md`, `AGENTS.md`, `ENGINEERING_QUALITY.md`, `AGENT_WORKFLOW.md`, active M7 `TODO.md`, relevant `STATUS.md`, the latest immutable `work-log/` entry, and live PR/CI state. Prior source evidence remains in older work logs.

The Narro worktrees are grouped under the Desktop folder `E:\SystemFiles\Desktop\Narro Project`. Inspect branch/dirty state before switching: the `Narro-m7-idle-performance` worktree contains three untracked icon outputs generated by local `npm run build`; they were excluded from source/docs commits and preserved after automatic approval review rejected their deletion. GitHub `main` remains the durable source truth.

## CURRENT MILESTONE

Milestone 7 — Floating Timer mode. Milestones 1–6 are complete; M7 items 1–6 and 13–14 are validated. Current compact progress: `6/10M || 5/5 | 8/14`. Items 7–12 retain physical Windows gates. The user authorized independent M7 work; do not advance to M8 before M7 acceptance is observed.

## CURRENT SOURCE AND VALIDATION

Latest main source: guarded squash merge `fce15f8ed7a70cd83e05f3b9448d627413b719e7` of [PR #143](https://github.com/MariosGiannakaras/Narro/pull/143), tree `608aec06f64a3184568bc0d171adc1b085220fa9` identical to exact PR head `9b80ee19c6678fca998b58740410f94c821cff26`. Windows CI #507 / run `36136277164` / job `108075004776` **PASS** (full preflight including the new Rust concurrency/retry/rollback tests, visual fixtures, Tauri release). Duplicate main CI #508 / run `36137982437` was cancelled after tree identity. Runtime artifact `10865303816`, digest `sha256:539f70706847c5938226a7dc9c65a31860c544b9d30b59f09d91620cd5fc52f3`. The shortcut registration race and UI retry-error reconciliation are automated-validated; this new executable has not had a physical shortcut retest.

Physical CI #503 retest on the actual Windows desktop confirmed three clean expand/collapse cycles after PR #138, including a visible subtask: no duplicated action strip or retained expanded DOM/pixels in the collapsed state. Panel/Timer continuity, shortcuts, same-monitor position after restart, topmost over maximized/F11 Edge, bottom work-area fit, and idle motion each received the scoped evidence recorded in `work-log/2026-09-25-codex-m7-physical-runtime-and-idle-recovery.md`. On CI #505, an expanded Timer whose task ended remained collapsible by actual keyboard interaction, and the native window returned to 356×118. PR #140 also removed an empty SQLite write reservation from every 250 ms notification check and added an executable lock-contention regression test. Do not infer from this alone that full idle CPU is solved; use the measurements in the work log.

## NEXT AGENT ACTION

1. Recheck main, local/unpublished state, open PRs, and CI. Read `work-log/2026-09-25-codex-m7-shortcut-retry-serialization.md` and the earlier physical/runtime log. Preserve any valid local state before cleanup.
2. Complete [the M7 Windows matrix](docs/M7_FLOATING_RUNTIME_VALIDATION.md) on exact source tree `608aec06` or a newer validated tree. Recheck basic Ctrl+Shift+T/P on the new #507 executable while testing conflict/retry and transition-boundary repetition. Remaining other conditions: continuous transition smoothness/OS reduced motion; secondary-monitor, no-saved-placement, scaling/resolution and non-default taskbar recovery; independent borderless/exclusive fullscreen. The current machine has only one 2560×1080/100% monitor. Avoid redoing the already observed three clean resize cycles.
3. Final-UI resource protocol is complete on CI #505: three valid idle runs per state plus a separate running-timer sample, with details and caveats in the earlier work log. The #143 registration change does not add long-lived runtime work; re-measure after performance-relevant source changes. Fix any observed source issue as a coherent slice with executable tests and one exact-head Windows CI run. Keep M8 blocked until M7 acceptance.

## USER ACTION REQUIRED

Physical Windows interaction/observation is required for the remaining M7 conditions. CI cannot prove compositor smoothness, shortcut ownership conflicts, or unavailable monitor/taskbar/DPI configurations. On this machine the Computer Use `sky` accessibility tree worked, but screenshot capture failed with `SetIsBorderRequired failed: No such interface supported (0x80004002)`; no new physical PASS was claimed. The #505 test process was stopped and the original user `narro.db` restored byte-for-byte. The user explicitly asked for a self-contained repository handoff so a later simple chat can continue from this checkpoint.

## INVARIANTS

Narro remains local-only Windows software with `main` plus reusable `focusSurface`. Rust/native owns timer/session, placement, DPI, and persistence. Mode changes, shortcuts, and placement recovery must preserve the live session. Display recovery remains event-driven; no continuous geometry or decorative animation loop. Keep physical and automated evidence distinct.
